# FEAT-005 固定受入Caseカタログ

## 固定方法

全caseは `testdata/fixtures/acceptance/knowledge-quality/cases.json` の一意な`case_id`として実装する。ここに示すseed ID、入力ID、期待oracle、Store差分、参照契約は、正規契約間の矛盾を是正する場合を除き変更しない。JSONの入れ子やtest helperの構造はImplementation領域が選ぶが、同じcase IDへ別の入力や合否規則を割り当ててはならない。

同じFixture領域に、Runtime受入評価の指示を置く。この指示は、選択`case_id`、渡す固定Claim／Episode、呼び出す既存Skill、必須の一時Markdown成果物、Case Resultへ写すoracleを定義する。Reading Valueへ固定Article Analysisを渡さず、A／G／I／Jは既存FEAT-003検証契約の節を`reading_value_reference`として参照する。通常のSkill本文へCase固有情報を追記せず、Runtime評価の成果物は呼出しsession内だけに保持する。自動CLI境界とRuntime受入評価の分離は[DEC-FEAT-017](../decisions/DEC-FEAT-017.md)を正とする。

`store_diff`の`none`は、Assertion、Evidence、Relation、revisionのいずれも増減・更新しないことを意味する。テスト同期用の管理ファイルはStore差分に含めず、Storeの外の隔離TempDirへ置く。`retain`は指定した既存IDが終了後にも参照可能であること、`add`は指定した新ID種別が追加されることを意味する。IDの具体値はFixture内でこのcatalogの論理IDへ固定対応させる。Case Resultの`not_executed_layers`は実行済み層と重複させず、対象外または停止後の層のみを記録する。

## 固定入力record

| Input ID | 固定値 |
| --- | --- |
| `ep-f` | `episode_id=ep-f`、`article_url=https://fixture.invalid/feat005/f`、`completed_at=2026-01-10T00:00:00Z`。contributionは`ordinal=1`、`source_type=message`、`observed_at=2026-01-09T23:59:00Z`、`source_text=unbuffered channelはなぜblockしますか？`の一件だけ。 |
| `ep-g` | `episode_id=ep-g`、`article_url=https://fixture.invalid/feat005/g`、`completed_at=2026-01-11T00:00:00Z`、`user_contributions=[]`。AI回答`ai-g`は`unbuffered channelのsendはreceiverまでblockする`であり、Episodeのfieldではない。 |
| `ep-h` | `episode_id=ep-h`、`article_url=https://fixture.invalid/feat005/h`、`completed_at=2026-01-12T00:00:00Z`。この時刻は既に完了したURL評価を表し、本caseはそのReading Valueを再実行しない。contributionは`ordinal=1`、`source_type=message`、`observed_at=2026-01-11T23:59:00Z`、`source_text=Goでは、先の「unbuffered channelのsendはreceiverが受信可能になる前にも完了する」は誤りで、receiverが受信可能になるまでblockする`の一件だけ。 |
| `cl-h` | `unbuffered channelのsendはreceiverが受信可能になるまでblockする`、Scope=`language=Go`、Temporal=`valid_from=2026-01-11T23:59:00Z`。更新後Storeをseedする再評価caseだけがこのClaimを評価し、Reading Valueを入力にも出力にも含めない。 |

| Case | Scenario | 実行層 | 固定seed / 入力 | 期待oracle・Store差分 | 参照契約 |
| --- | --- | --- | --- | --- | --- |
| `FEAT005-A-SEARCH-EMPTY` | A | `cli_store`, `knowledge_search`, `end_to_end` | `seed-empty`; Claim `cl-a: Goのmapは並行書込み安全である`。`reading_value_reference=FEAT-003 V-002`。 | Assessment=`no_evidence`、Confidence=`low`、Knownは空、Gapは「Storeに判断根拠なし」。Traceは直接探索と`no_expandable_path`。`reading_value_reference=FEAT-003 V-002`。`store_diff:none`。 | FEAT-002 状態分類・停止、FEAT-003 V-002 |
| `FEAT005-B-SEARCH-EXACT` | B | `cli_store`, `knowledge_search` | `seed-b-exact`: Assertion `as-b` は`cl-b: unbuffered channelへのsendはreceiverが受信可能になるまでblockする`と意味同一、Evidence `ev-b` は同じ説明の`user_reasoning`・`strong`。 | Assessment=`known`、Confidence=`high`、Knownに`as-b`/`ev-b`、Traceの直接探索結果に両ID、停止=`strong_conclusion`。`store_diff:none`。 | FEAT-002 状態分類 |
| `FEAT005-C-SEARCH-PARTIAL` | C | `cli_store`, `knowledge_search` | `seed-c-components`: Assertion/Evidence `as-c-send`（sendは同期点になり得る）と`as-c-recv`（receiveは待機する）。Target `cl-c: unbuffered channelはsendとreceiveを同期する`。Relationは置かない。 | Assessment=`partially_known`、Confidence=`medium`。Knownは二つの構成知識、Gapはsend/receive間の関係。直接Evidenceも導出Relationもないため`inferable`にしない。Traceは構成探索後`no_expandable_path`。`store_diff:none`。 | FEAT-002 状態分類・構成知識case |
| `FEAT005-D-SEARCH-CONTRADICTED` | D | `cli_store`, `knowledge_search` | `seed-d-contradiction`: `as-d`/`ev-d`は「unbuffered channelへのsendは常に即時に完了する」という`user_reasoning`・`strong`。Target `cl-d`はBと同じ正しい命題。 | Assessment=`contradicted`、Confidence=`high`、Contradictionsに`as-d`/`ev-d`、Trace停止=`strong_conclusion`。同等以上の現行支持Evidenceをseedしない。`store_diff:none`。 | FEAT-002 状態分類 |
| `FEAT005-E-SEARCH-OUTDATED` | E | `cli_store`, `knowledge_search` | `seed-e-temporal`: Target `cl-e=Go 1.22+ではrange変数をgoroutine内で捕捉しても各iterationは別の変数を使う`、Scope=`language=Go`、Temporal=`version_scope=go1.22+`。`as-e`は同一主題・Scopeの旧理解、`ev-e-old`はkind=`user_reasoning`、raw text=`Go 1.21ではrange変数をgoroutineで捕捉すると全goroutineが同じ変数を参照する`、`observed_at=2023-10-01T00:00:00Z`、`version_scope=go1.21`。`ev-e-correction`はkind=`correction`、raw text=`Go 1.22でrange変数の仕様が変わったため、上のGo 1.21の理解をGo 1.22+へ適用しない`、`observed_at=2024-02-01T00:00:00Z`、`version_scope=go1.22+`。 | Assessment=`outdated`、Confidence=`high`、Temporal Issuesに両Evidence、Traceは時点探索を含み停止=`strong_conclusion`。訂正Evidenceは旧理解の適用終了だけを示し、現行Targetを直接支持する同等以上Evidenceをseedしない。`store_diff:none`。 | FEAT-002 状態分類・Temporal探索 |
| `FEAT005-F-ACQUISITION-QUESTION` | F | `knowledge_acquisition`, `knowledge_update`, `end_to_end` | `seed-empty`; 固定入力record `ep-f`。 | Acquisition Candidate=`[]`。Update ResultはDecision=`[]`、status=`completed`、CLI operations=`[]`。更新層はCandidate不在を理由にmutationを開始しない。`store_diff:none`。 | FEAT-004 Candidate入力境界・Update Result |
| `FEAT005-G-ACQUISITION-AI-ONLY` | G | `knowledge_acquisition`, `knowledge_update`, `end_to_end` | `seed-empty`; 固定入力record `ep-g`。`reading_value_reference=FEAT-003 V-004`。 | Acquisition Candidate=`[]`。Update ResultはDecision=`[]`、status=`completed`、CLI operations=`[]`。`reading_value_reference=FEAT-003 V-004`。`store_diff:none`。 | FEAT-003 V-004、FEAT-004 Episode境界 |
| `FEAT005-H-UPDATE-CORRECTION` | H | `cli_store`, `knowledge_acquisition`, `knowledge_update`, `end_to_end` | `seed-h-prior`: `as-h`はidentity=`subject=unbuffered-channel-send`, Scope=`language=Go`の旧本文`unbuffered channelのsendはreceiverが受信可能になる前にも完了する`、revision `rev-h-1`、Relation `rel-h`。`ev-h-prior`はkind=`user_reasoning`、raw text=`unbuffered channelのsendはreceiverが受信可能になる前にも完了する`、`observed_at=2025-12-01T00:00:00Z`、対象Assertion=`as-h`。固定入力record `ep-h`。 | AcquisitionはCandidate `cand-h-1`（kind=`correction`, strength=`strong`、proposed assertion=`cl-h`、evidence raw textは`ep-h`のsource_text、scope=`[language=Go]`、temporal=`null`、search queries=`[cl-h, 引用された旧命題]`）を1件だけ作る。Update Decisionは最初の`search-text(query=cl-h, result_ids=[])`の後、引用された旧命題の`search-text(result_ids=[as-h])`、`get(id=as-h, result_ids=[as-h])`、`get-evidence(assertion_id=as-h, result_ids=[ev-h-prior])`を先行させた後、action=`revise`, target=`as-h`, status=`applied`とする。その`cli_operations`は`search-text`、`search-text`、`get`、`get-evidence`、`revise(new revision=rev-h-2)`、`attach-evidence(assertion_id=as-h, new evidence=ev-h-correction)`の順に固定する。`get`応答の履歴とStore oracleで`rev-h-1`を確認する。`ev-h-correction`はkind=`correction`、raw text=`ep-h`のsource_text、`observed_at=2026-01-11T23:59:00Z`、対象Assertion=`as-h`。`store_diff:retain(ev-h-prior, rev-h-1, rel-h); add(rev-h-2, ev-h-correction)`。 | FEAT-004 correction/revise/attach-evidence、DEC-FEAT-022 |
| `FEAT005-H-SEARCH-CORRECTED` | H | `cli_store`, `knowledge_search` | `seed-h-corrected`は直前caseの期待Storeと同一の`as-h`、`rev-h-2`、`ev-h-correction`、および保持済み`ev-h-prior`/`rev-h-1`/`rel-h`を持つ。EvidenceはいずれもAssertion `as-h`へ紐付く。Targetは固定入力record `cl-h`。 | Assessment=`known`、Confidence=`high`、Knownに`as-h`/`ev-h-correction`、Trace停止=`strong_conclusion`。旧Evidenceは旧本文・時点を持つまま保持され、現行revisionと訂正Evidenceの評価根拠を削除しない。`store_diff:none`。 | FEAT-002 状態分類、FEAT-004 履歴保全 |
| `FEAT005-I-READ-SELECTED` | I | `cli_store`, `knowledge_search`, `end_to_end` | `seed-i-known`: `cl-i-1`と`cl-i-3`にstrongな完全一致Evidence。`cl-i-2`には正常Assessment=`no_evidence`を固定する。`reading_value_reference=FEAT-003 V-002`。 | `cl-i-1`／`cl-i-3`は`known`、`cl-i-2`は`no_evidence`。各Search Traceは固定Claimの探索結果と停止理由を持つ。`reading_value_reference=FEAT-003 V-002`。`store_diff:none`。 | FEAT-002 状態分類・Trace、FEAT-003 V-002 |
| `FEAT005-J-READ-TRIVIAL` | J | `cli_store`, `knowledge_search`, `end_to_end` | `seed-j-known`: 主要Claimはstrong Evidenceで既知、補足Claim `cl-j-3`だけを低重要度・低適用性の`no_evidence`とする。`reading_value_reference=FEAT-003 V-002`。 | 主要Claimは`known`、`cl-j-3`は`no_evidence`。各Search Traceは固定Claimの探索結果と停止理由を持つ。`reading_value_reference=FEAT-003 V-002`。`store_diff:none`。 | FEAT-002 状態分類・Trace、FEAT-003 V-002 |
| `FEAT005-X-SEARCH-TECHNICAL-FAILURE` | B（失敗派生） | `cli_store`, `knowledge_search` | `scenario_id=B`。`seed-b-exact`と`cl-b`。最初の`search-text`を既存error JSON `storage_error`・非130 exitで失敗させるプロセスFixture。 | 部分Traceはoperation=`search-text`、error code=`storage_error`、exit code、停止=`technical_failure`を持つ。Assessment、Acquisition、Updateはすべて未生成／未実行。CLI mutation=0、`store_diff:none`。 | FEAT-001 error JSON、FEAT-002 technical_failure |
| `FEAT005-X-SEARCH-CANCELED` | B（中断派生） | `cli_store`, `knowledge_search` | `scenario_id=B`。`seed-b-exact`と`cl-b`。最初の`search-text`をresponse開始前・無出力のexit 130で終了させるプロセスFixture。 | 部分Traceはoperation=`search-text`とexit code=`130`だけを持ち、error codeを持たず停止=`canceled`。Assessment、Acquisition、Updateはすべて未生成／未実行。CLI mutation=0、`store_diff:none`。親Orchestrationも中断済みの場合だけ、部分Traceを出力・保存しない。 | FEAT-001 exit 130、FEAT-002 canceled |

`FEAT005-X-*`はA〜Jの意味を変更しない、既存Search停止契約を横断的に検証する派生caseである。各caseの後続未実行はcase resultの`not_executed_layers`にも必ず記録する。
