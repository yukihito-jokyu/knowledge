# Codex作業規則

## GitHub認証

GitHubの認証状態は、Taskの開始、ローカルCommit、worktree管理の前提条件ではない。認証が必要なGitHub操作が失敗した場合は、失敗した操作と実際のエラーを利用者へ報告する。`gh auth login`などの認証操作を、失敗理由から推測して案内・実行しない。認証が必要な外部操作を利用者が明示的に依頼し、実行に必要な認証方法も利用者が指定した場合だけ、その指定に従う。

## 利用者の指示からSkill改善案を作る条件

Skillは、Codexに特定作業で再利用させる手順書である。利用者の指示が、今回実際に使用するSkillの規則を補足、訂正、上書き、または例外化した場合は、`$skill-feedback`を使用する。利用者がSkillの不足を明示した場合や、Skillに従った結果へ再利用可能な手順変更を求めた場合も使用する。

一回限りの成果物指定、今回だけの好み、対象Skillを実際に使用していない依頼では使用しない。

## 提示と反映の順序

主依頼の成果物作成と検証を先に完了し、その後で利用者の指示と実際に使用したSkillを比較して修正案を提示する。主依頼が未完了または回答待ちなら、修正案の最終提示を行わない。

修正案には、対象Skill、根拠となった指示、現在の規則との差、変更後の動作、将来も必要な理由、影響を含める。修正候補がない場合も、その理由を明記する。

利用者が修正案の適用を明示的に承認するまで、Skillとこの呼び出し規則を変更しない。ただし、主依頼そのものが「対象Skillをこの内容で変更する」という明示指示なら、その指示済みの範囲へ追加承認を求めず変更する。承認済みのSkill変更では`$skill-creator`を使用し、構造検証と必要な動作確認を行う。
